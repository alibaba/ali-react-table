---
id: extends
title: 实现自定义的表格拓展(todo)
---

## 处理受控状态 todo

## 处理非受控状态 todo

## 相关 API

`pipeline.ensurePrimaryKey(hint?: string): PrimaryKey`
确保 primaryKey 已被设置，并返回 primaryKey

pipeline.state

pipeline.setState

pipeline.setStateAtKey 将 stateKey 对应的状态设置为 partialState
